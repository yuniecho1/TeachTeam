export enum UserType {
    ADMIN = 'admin',
    LECTURER = 'lecturer',
    CANDIDATE = 'candidate'
  }
  
  export enum Availability {
    PART_TIME = 'part time',
    FULL_TIME = 'full time'
  }
  
  export enum AppliedRole {
    LAB_ASSISTANT = 'lab assistant',
    TUTOR = 'tutor'
  }